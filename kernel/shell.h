#ifndef SHELL_H
#define SHELL_H

// Keep your existing includes (like types.h)
#include "types.h"

// Keep all your existing function declarations from Stage 0, 1, and 2 here.
// For example:
// void shell_init(void);
// void cmd_help(void);
// void cmd_ps(void);

// Add the new command declarations for Stage 3 below:
void cmd_meminfo(void);
void cmd_test_pmm(void);

#endif